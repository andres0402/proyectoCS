<div class="block mx-auto my-12 p-8 bg-white w-3/4 border border-gray-200 rounded-lg shadow-lg text-gray-800">
<h1 class="text-center text-2xl font-bold text-blue">Información de la propuesta</h1>
  {{#each props}}
    <div class="mr-auto">
        <p class="text-2xl py-4">Titulo: {{title}}</p>
        <p class="text-2xl py-4">Descripcion: {{description}}</p>
        <p class="text-2xl py-4">Sector: {{sector}}</p>
        <p class="text-2xl py-4">Segmento: {{segment}}</p>
        <p class="text-2xl py-4">Votos totales: {{votos}}</p>
      </div>
{{/each}}


</div>
<div class="block mx-auto my-12 p-1 w-1/3 text-gray-800">
{{#each props}}
  <form action="/vote/{{id}}">
  <button class="rounded-md bg-blue-500 w-full text-lg text-white font-semibold p-2 my-3 hover:bg-blue-600 focus:bg-blue-700">Votar</button>
  </form>
{{/each}}
  {{#each props}}
  <form action="/edit/{{id}}">
  <button href="/" class="rounded-md bg-blue-500 w-full text-lg text-white font-semibold p-2 my-3 hover:bg-blue-600 focus:bg-blue-700">Agregar mejora</button>
  </form>
  {{/each}}
  {{#each props}}
  <form action="/obtenerprop/{{sector}}">
  <button href="/" class="rounded-md bg-blue-500 w-full text-lg text-white font-semibold p-2 my-3 hover:bg-blue-600 focus:bg-blue-700">Regresar</button>
  </form>
  {{/each}}
  </div>